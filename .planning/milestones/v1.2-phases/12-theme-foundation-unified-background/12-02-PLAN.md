---
phase: 12-theme-foundation-unified-background
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/components/sections/Skills.tsx
  - src/components/sections/Tooling.tsx
  - src/components/sections/WhoAmI.tsx
  - src/components/sections/Timeline.tsx
  - src/components/sections/Contact.tsx
  - src/components/layout/Navigation.tsx
  - src/components/layout/MobileMenu.tsx
  - src/components/layout/NavDropdown.tsx
  - src/components/projects/ProjectCard.tsx
  - src/components/pdf/PdfViewer.tsx
autonomous: true
requirements:
  - THEME-03
  - THEME-06

must_haves:
  truths:
    - "Scrolling from hero through all sections to footer shows one continuous background with no hard color breaks"
    - "No section renders NoisyBackground or AnimatedGridPattern wrappers"
    - "Contact section has no gradient overlay div"
    - "Navigation, mobile menu, and nav dropdown render correctly in both light and dark modes"
    - "Project cards render correctly in both light and dark modes"
    - "PDF viewer toolbar and controls are readable in both light and dark modes"
  artifacts:
    - path: "src/components/sections/Skills.tsx"
      provides: "Skills section without NoisyBackground wrapper"
    - path: "src/components/sections/Tooling.tsx"
      provides: "Tooling section without NoisyBackground wrapper"
    - path: "src/components/sections/WhoAmI.tsx"
      provides: "WhoAmI section without NoisyBackground wrapper"
    - path: "src/components/sections/Timeline.tsx"
      provides: "Timeline section without AnimatedGridPattern"
    - path: "src/components/sections/Contact.tsx"
      provides: "Contact section without gradient overlay"
    - path: "src/components/pdf/PdfViewer.tsx"
      provides: "PDF viewer with dark mode compatible toolbar"
  key_links:
    - from: "src/components/sections/*.tsx"
      to: "body gradient in app.css"
      via: "transparent section backgrounds reveal the unified body gradient"
      pattern: "bg-transparent|no background class"
    - from: "src/components/pdf/PdfViewer.tsx"
      to: "shadcn Dialog/Drawer"
      via: "Dialog/Drawer chrome auto-themed; toolbar uses semantic tokens or dark: variants"
      pattern: "dark:|bg-background|text-foreground|text-muted-foreground"
---

<objective>
Remove all per-section background effects to create a unified continuous background, and update all site-facing components (navigation, cards, PDF viewer) to render correctly in both light and dark modes.

Purpose: Eliminates visual seams between sections and ensures every visitor-facing component adapts to the active theme.
Output: A seamless page-spanning gradient visible through all sections, with all site components correctly themed in both modes.
</objective>

<execution_context>
@C:/Users/Jack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-theme-foundation-unified-background/12-CONTEXT.md
@.planning/phases/12-theme-foundation-unified-background/12-RESEARCH.md
@.planning/phases/12-theme-foundation-unified-background/12-01-SUMMARY.md

@src/components/sections/Skills.tsx
@src/components/sections/Tooling.tsx
@src/components/sections/WhoAmI.tsx
@src/components/sections/Timeline.tsx
@src/components/sections/Contact.tsx
@src/components/layout/Navigation.tsx
@src/components/layout/MobileMenu.tsx
@src/components/layout/NavDropdown.tsx
@src/components/projects/ProjectCard.tsx
@src/components/pdf/PdfViewer.tsx

<interfaces>
<!-- Plan 01 establishes these CSS foundations that this plan relies on -->

From src/styles/app.css (after Plan 01):
- .dark class block with all semantic tokens
- @custom-variant dark (&:where(.dark, .dark *)) -- enables dark: prefix
- body has linear-gradient using --gradient-top and --gradient-bottom
- --color-silicon-* scale has blue chroma in both modes
- Semantic tokens: bg-background, bg-card, text-foreground, text-muted-foreground, border-border

Key token mapping for site components:
- bg-white      -> bg-background (or bg-card for elevated surfaces)
- bg-white/80   -> bg-background/80
- bg-white/90   -> bg-background/90
- bg-white/95   -> bg-background/95
- text-silicon-600 -> text-muted-foreground (for dark mode compatibility)
- border-silicon-200/30 -> border-border/30
- hover:bg-silicon-50 -> hover:bg-muted
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove per-section backgrounds and create unified transparent sections</name>
  <files>src/components/sections/Skills.tsx, src/components/sections/Tooling.tsx, src/components/sections/WhoAmI.tsx, src/components/sections/Timeline.tsx, src/components/sections/Contact.tsx</files>
  <action>
**Skills.tsx:**
1. Remove the import: `import { NoisyBackground } from '@/components/effects/NoisyBackground';`
2. Replace the `<NoisyBackground ...>` wrapper with a plain `<section>` tag. Keep the section's id, className (py-24 padding, etc.), and all child content intact. Do NOT add any background class -- the section should be transparent so the body gradient shows through.
3. Keep the component file for NoisyBackground itself (it gets deleted in Phase 15).

**Tooling.tsx:**
1. Remove the import: `import { NoisyBackground } from '@/components/effects/NoisyBackground';`
2. Replace `<NoisyBackground ...>` wrapper with a plain `<section>` tag, same approach as Skills. Preserve id, padding classes, all children.

**WhoAmI.tsx:**
1. Remove the import: `import { NoisyBackground } from '@/components/effects/NoisyBackground';`
2. Replace `<NoisyBackground ...>` wrapper with a plain `<section>` tag. Preserve id, padding, children.

**Timeline.tsx:**
1. Remove the import: `import { AnimatedGridPattern } from '../effects/AnimatedGridPattern';`
2. Remove the `<AnimatedGridPattern ... />` self-closing element from the JSX. It's rendered inside the section as a background effect overlay -- just delete the element and its container div if it has one.
3. Keep the section transparent (no background class).

**Contact.tsx:**
1. Find the absolute-positioned gradient overlay div with classes including `bg-gradient-to-b from-cleanroom via-uw-purple-faint/30 to-cleanroom` (around line 17-18).
2. Remove that entire `<div>` element. The section should be transparent.
3. If the section has `relative` positioning only for the overlay, check if it's still needed for other children before removing.

For ALL five sections: ensure section separation is via whitespace/padding only (py-24 pattern). No divider lines, no borders, no background colors. Sections should be fully transparent.
  </action>
  <verify>
    <automated>npx vitest run -x &amp;&amp; node -e "const fs=require('fs'); const files=['src/components/sections/Skills.tsx','src/components/sections/Tooling.tsx','src/components/sections/WhoAmI.tsx']; let ok=true; files.forEach(f=>{const c=fs.readFileSync(f,'utf8'); if(c.includes('NoisyBackground')){console.error(f+' still has NoisyBackground'); ok=false;}}); const tl=fs.readFileSync('src/components/sections/Timeline.tsx','utf8'); if(tl.includes('AnimatedGridPattern')){console.error('Timeline still has AnimatedGridPattern'); ok=false;} const ct=fs.readFileSync('src/components/sections/Contact.tsx','utf8'); if(ct.includes('from-cleanroom via-uw-purple-faint')){console.error('Contact still has gradient overlay'); ok=false;} if(ok) console.log('All section backgrounds removed'); else process.exit(1);"</automated>
  </verify>
  <done>NoisyBackground removed from Skills, Tooling, WhoAmI. AnimatedGridPattern removed from Timeline. Gradient overlay removed from Contact. All sections are transparent with py-24 spacing only.</done>
</task>

<task type="auto">
  <name>Task 2: Theme site components and PDF viewer for dark mode</name>
  <files>src/components/layout/Navigation.tsx, src/components/layout/MobileMenu.tsx, src/components/layout/NavDropdown.tsx, src/components/projects/ProjectCard.tsx, src/components/pdf/PdfViewer.tsx</files>
  <action>
**Navigation.tsx:**
- Replace `bg-white/80` with `bg-background/80` on the fixed nav container
- Replace `border-silicon-200/30` with `border-border/30` if present
- Check for any `text-ink` or hardcoded text colors and ensure they work in dark mode (text-ink already has dark override from Plan 01, so it should be fine)

**MobileMenu.tsx:**
- Replace `bg-white/95` with `bg-background/95` on the mobile menu overlay

**NavDropdown.tsx:**
- Replace `bg-white/90` with `bg-background/90` on the dropdown panel
- Replace `border-silicon-200/30` with `border-border/30` if present

**ProjectCard.tsx:**
- Replace `bg-white` with `bg-card` on the card container
- Check for any hardcoded text colors and update if needed (text-ink is fine, but text-silicon-* may need dark: variants)

**PdfViewer.tsx:**
The Dialog/Drawer components from shadcn auto-theme via CSS variables. The toolbar needs updates:
- The shared `btnClass` uses `hover:bg-silicon-50 text-silicon-600`. These custom palette tokens now have dark mode overrides from Plan 01, but for better semantic clarity, update:
  - `hover:bg-silicon-50` -> `hover:bg-muted`
  - `text-silicon-600` -> `text-muted-foreground`
- The toolbar border `border-silicon-200/30` -> `border-border/30`
- The page count and zoom text `text-silicon-600` -> `text-muted-foreground`
- The error/loading text `text-silicon-600` -> `text-muted-foreground`
- The download link `text-accent` should work (accent token has dark override), verify it's readable

After changes, the PdfViewer toolbar should use only semantic tokens (bg-background, text-foreground, text-muted-foreground, border-border, hover:bg-muted) so it auto-themes via the shadcn token system.
  </action>
  <verify>
    <automated>npx vitest run -x &amp;&amp; npx tsc --noEmit &amp;&amp; node -e "const fs=require('fs'); const checks=[['src/components/layout/Navigation.tsx','bg-white/80'],['src/components/layout/MobileMenu.tsx','bg-white/95'],['src/components/layout/NavDropdown.tsx','bg-white/90'],['src/components/projects/ProjectCard.tsx','bg-white']]; let ok=true; checks.forEach(([f,pat])=>{const c=fs.readFileSync(f,'utf8'); if(c.includes(pat)){console.error(f+' still has '+pat); ok=false;}}); if(ok) console.log('All site components themed'); else process.exit(1);"</automated>
  </verify>
  <done>Navigation uses bg-background/80, MobileMenu uses bg-background/95, NavDropdown uses bg-background/90, ProjectCard uses bg-card, PdfViewer toolbar uses semantic tokens. All site components render correctly in both light and dark modes.</done>
</task>

</tasks>

<verification>
- `npx vitest run -x` -- all tests pass
- `npx tsc --noEmit` -- no TypeScript errors
- No imports of NoisyBackground in section components
- No imports of AnimatedGridPattern in Timeline
- No gradient overlay div in Contact
- No bg-white references in Navigation, MobileMenu, NavDropdown, ProjectCard
- PdfViewer uses only semantic tokens for colors
</verification>

<success_criteria>
1. Scrolling the page shows one continuous gradient background through all sections
2. No section has its own background effect (NoisyBackground, AnimatedGridPattern, gradient overlay)
3. Navigation, MobileMenu, NavDropdown use bg-background/* instead of bg-white/*
4. ProjectCard uses bg-card instead of bg-white
5. PdfViewer toolbar uses semantic tokens and is readable in both modes
6. TypeScript compiles with no errors
7. All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-theme-foundation-unified-background/12-02-SUMMARY.md`
</output>

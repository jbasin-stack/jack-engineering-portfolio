---
phase: 15-contact-footer-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/Contact.tsx
  - src/components/layout/Footer.tsx
  - src/App.tsx
autonomous: true
requirements: [CTFT-01, CTFT-02, CTFT-03]

must_haves:
  truths:
    - "Contact section displays 4 equal links (Email, GitHub, LinkedIn, Resume) in a horizontal row with icon + label text"
    - "Hovering any contact link shifts icon + label from muted gray to accent blue with 300ms transition"
    - "Resume link triggers a file download (not a PDF viewer modal)"
    - "Footer shows dynamic copyright year and 'Built with React & Motion' tagline"
    - "A subtle 1px border separates contact section from footer"
  artifacts:
    - path: "src/components/sections/Contact.tsx"
      provides: "Refactored contact section with 4 equal links in horizontal row"
      contains: "Say Hello"
    - path: "src/components/layout/Footer.tsx"
      provides: "Minimal footer with copyright and tagline"
      contains: "getFullYear"
    - path: "src/App.tsx"
      provides: "Footer component rendered as sibling to Contact inside <footer>"
      contains: "Footer"
  key_links:
    - from: "src/components/sections/Contact.tsx"
      to: "src/data/contact.ts"
      via: "contactData import for email, resumePath, socialLinks"
      pattern: "contactData\\."
    - from: "src/App.tsx"
      to: "src/components/layout/Footer.tsx"
      via: "Footer import rendered inside <footer> element"
      pattern: "<Footer"
---

<objective>
Refactor Contact.tsx into a clean centered section with "Say Hello" heading, tagline, and a single horizontal row of 4 equal links (Email, GitHub, LinkedIn, Resume) with icon + label hover animations. Create a separate Footer.tsx component with dynamic copyright year and "Built with React & Motion" tagline, rendered as a sibling to Contact inside the existing `<footer>` element in App.tsx.

Purpose: Complete the contact and footer UI for the portfolio's final section, giving visitors clear access to all contact channels with a polished, minimal sign-off.
Output: Refactored Contact.tsx, new Footer.tsx, updated App.tsx
</objective>

<execution_context>
@C:/Users/Jack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/sections/Contact.tsx
@src/data/contact.ts
@src/styles/motion.ts
@src/App.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/data.ts:
```typescript
export interface SocialLink {
  platform: string;
  url: string;
  icon: string;
}

export interface ContactData {
  tagline: string;
  email: string;
  resumePath: string;
  socialLinks: SocialLink[];
}
```

From src/data/contact.ts:
```typescript
export const contactData: ContactData = {
  tagline: 'Open to internship and research opportunities',
  email: 'jbasin@uw.edu',
  resumePath: '/resume.pdf',
  socialLinks: [
    { platform: 'GitHub', url: 'https://github.com/jackbasinski', icon: 'Github' },
    { platform: 'LinkedIn', url: 'https://linkedin.com/in/jackbasinski', icon: 'Linkedin' },
  ],
};
```

From src/styles/motion.ts:
```typescript
export const sectionVariants: Variants = {
  hidden: {},
  visible: { transition: { staggerChildren: 0.1 } },
};

export const fadeUpVariant: Variants = {
  hidden: { opacity: 0, y: 20 },
  visible: { opacity: 1, y: 0, transition: { duration: 0.6, ease: easing.out } },
};
```

From src/App.tsx (lines 77-79):
```typescript
<footer>
  <Contact />
</footer>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Contact.tsx and create Footer.tsx</name>
  <files>src/components/sections/Contact.tsx, src/components/layout/Footer.tsx</files>
  <action>
**Refactor Contact.tsx:**

1. Remove all imports that are no longer needed:
   - Remove `import { useState } from 'react'`
   - Remove `import { LazyPdfViewer } from '../pdf/LazyPdfViewer'`
   - Replace `Download, Eye` lucide imports with `FileText`

2. Update the icon map to include all 4 icons:
   ```typescript
   import { Mail, Github, Linkedin, FileText } from 'lucide-react';
   const iconMap = { Mail, Github, Linkedin, FileText } as const;
   ```

3. Remove the `useState(false)` hook for `showResume`.

4. Build a unified links array inside the component (do NOT modify the data file):
   ```typescript
   const links = [
     { label: 'Email', href: `mailto:${contactData.email}`, icon: 'Mail', external: false },
     { label: 'GitHub', href: contactData.socialLinks[0].url, icon: 'Github', external: true },
     { label: 'LinkedIn', href: contactData.socialLinks[1].url, icon: 'Linkedin', external: true },
     { label: 'Resume', href: contactData.resumePath, icon: 'FileText', download: true },
   ];
   ```

5. Change the h2 heading text from "Get in Touch" to "Say Hello" per user decision.

6. Replace the entire body (address block, resume actions div, social links div, and LazyPdfViewer) with a single `motion.div` containing a horizontal flex row of 4 links. Each link:
   - Uses `inline-flex items-center gap-2` for icon + label alignment
   - Base color: `text-silicon-400` (muted gray)
   - Hover color: `hover:text-accent` (oklch accent blue)
   - Transition: `transition-colors duration-300` (300ms per user decision)
   - No scale, bounce, or glow effects -- color shift only per user decision
   - External links get `target="_blank" rel="noopener noreferrer"`
   - Resume link gets the `download` attribute
   - Icon rendered at `size={18} strokeWidth={1.5}` (existing pattern)

7. Remove the `<address>` semantic wrapper (no longer appropriate for mixed links).

8. The link row container should use `flex flex-wrap items-center justify-center gap-6 sm:gap-8` for responsive wrapping.

9. Keep the existing `sectionVariants` / `fadeUpVariant` / `whileInView` pattern exactly as-is for section entry animation.

**Create Footer.tsx:**

1. Create `src/components/layout/Footer.tsx` as a new component.

2. Structure:
   ```
   div (border-t border-border py-8 text-center)
     p "copy {year} Jack Basinski" (text-sm text-silicon-400)
     p "Built with React & Motion" (text-xs text-silicon-400/60 mt-1)
   ```

3. Use `new Date().getFullYear()` for dynamic year -- do NOT hardcode 2026.

4. "Built with React & Motion" is plain text, no links (per research recommendation: footer should feel like a quiet sign-off, not a destination).

5. Keep it simple -- no motion animations on the footer itself. It's a static sign-off.
  </action>
  <verify>
    <automated>cd "C:/Claude code projects/GSD Projects/Jack_Engineering_Portfolio" && npx tsc -b --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Contact.tsx renders "Say Hello" heading, tagline, and 4 equal links in a horizontal row with icon + label and hover:text-accent transition. Footer.tsx renders dynamic copyright year and "Built with" tagline with border-top separator. No useState, no LazyPdfViewer, no PDF modal in Contact.tsx. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Footer into App.tsx</name>
  <files>src/App.tsx</files>
  <action>
1. Add import for the new Footer component:
   ```typescript
   import { Footer } from './components/layout/Footer';
   ```

2. Update the `<footer>` element (currently around line 77-79) to include Footer as a sibling below Contact:
   ```tsx
   <footer>
     <Contact />
     <Footer />
   </footer>
   ```

3. That's it -- minimal change. The footer element already exists and provides semantic context.
  </action>
  <verify>
    <automated>cd "C:/Claude code projects/GSD Projects/Jack_Engineering_Portfolio" && npx tsc -b --noEmit && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>App.tsx renders Footer below Contact inside the existing footer element. Production build succeeds. The page ends with contact links followed by copyright footer.</done>
</task>

</tasks>

<verification>
1. `npx tsc -b --noEmit` passes with no type errors
2. `npx vite build` succeeds
3. Contact.tsx contains "Say Hello", not "Get in Touch"
4. Contact.tsx does NOT contain `LazyPdfViewer`, `useState`, `showResume`
5. Contact.tsx contains `hover:text-accent` and `transition-colors duration-300`
6. Footer.tsx contains `getFullYear()` and "Built with React & Motion"
7. App.tsx contains `<Footer />` inside the `<footer>` element
</verification>

<success_criteria>
- Contact section shows "Say Hello" heading with 4 equal icon+label links in a horizontal row
- All links use muted-to-accent color hover transition at 300ms
- Resume link has download attribute (no PDF modal)
- Footer displays dynamic year copyright and "Built with" tagline
- 1px border-top separates contact from footer
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/15-contact-footer-cleanup/15-01-SUMMARY.md`
</output>

---
phase: 15-contact-footer-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/components/effects/NoisyBackground.tsx
  - src/components/effects/AnimatedGridPattern.tsx
  - src/components/effects/AuroraBackground.tsx
  - src/components/effects/Particles.tsx
  - src/components/effects/__tests__/effects.test.ts
  - src/tests/bundle.test.ts
  - src/styles/__tests__/colors.test.ts
  - src/styles/app.css
autonomous: true
requirements: [CTFT-03]

must_haves:
  truths:
    - "Production build succeeds with no references to NoisyBackground, AnimatedGridPattern, AuroraBackground, or Particles"
    - "All vitest tests pass after deprecated component removal"
    - "CardSpotlight still exists and its test still passes"
    - "No aurora CSS remains in app.css"
  artifacts:
    - path: "src/components/effects/CardSpotlight.tsx"
      provides: "Only remaining effect component (still used by ProjectCard)"
      min_lines: 10
    - path: "src/components/effects/__tests__/effects.test.ts"
      provides: "Effects test reduced to CardSpotlight only"
      contains: "CardSpotlight"
  key_links:
    - from: "src/components/effects/__tests__/effects.test.ts"
      to: "src/components/effects/CardSpotlight.tsx"
      via: "import and function-type assertion"
      pattern: "CardSpotlight"
---

<objective>
Delete 4 unused effect components (NoisyBackground, AnimatedGridPattern, AuroraBackground, Particles), remove aurora CSS from app.css, update all affected test files to remove references to deleted code, and verify the production build is clean.

Purpose: Remove dead code accumulated from Phase 12-13 redesign. The codebase should have no unused effect components and no failing tests. This is the final cleanup step for v1.2.
Output: 4 files deleted, 3 test files updated, 1 CSS file cleaned, production build verified
</objective>

<execution_context>
@C:/Users/Jack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/effects/__tests__/effects.test.ts
@src/tests/bundle.test.ts
@src/styles/__tests__/colors.test.ts
@src/styles/app.css

<interfaces>
<!-- Files being deleted (executor needs to know what's being removed) -->

Files to DELETE:
- src/components/effects/NoisyBackground.tsx
- src/components/effects/AnimatedGridPattern.tsx
- src/components/effects/AuroraBackground.tsx
- src/components/effects/Particles.tsx

File to KEEP:
- src/components/effects/CardSpotlight.tsx (used by ProjectCard)

Current effects.test.ts imports all 5 components -- must be reduced to CardSpotlight only.

Current bundle.test.ts (lines 21-26) asserts Contact.tsx imports LazyPdfViewer -- this assertion must be removed since Plan 01 removed LazyPdfViewer from Contact.tsx. The PapersSection LazyPdfViewer assertion (lines 14-18) remains valid.

Current colors.test.ts (lines 89-101) asserts aurora keyframes exist in app.css -- these assertions must be removed since aurora CSS is being deleted.

Aurora CSS in app.css to remove:
- Lines 29-34: `--animate-aurora` custom property and `@keyframes aurora` definition
- Lines 271-273: `.animate-aurora { animation: none; }` inside the prefers-reduced-motion media query
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete deprecated effect components and clean aurora CSS</name>
  <files>src/components/effects/NoisyBackground.tsx, src/components/effects/AnimatedGridPattern.tsx, src/components/effects/AuroraBackground.tsx, src/components/effects/Particles.tsx, src/styles/app.css</files>
  <action>
1. Delete the following 4 files:
   - `src/components/effects/NoisyBackground.tsx`
   - `src/components/effects/AnimatedGridPattern.tsx`
   - `src/components/effects/AuroraBackground.tsx`
   - `src/components/effects/Particles.tsx`

2. Edit `src/styles/app.css` to remove aurora-related CSS:
   - Remove the `/* Aurora animation */` comment, `--animate-aurora` custom property definition (line 30), and the `@keyframes aurora` block (lines 31-34)
   - Remove the `.animate-aurora { animation: none; }` rule inside the `@media (prefers-reduced-motion: reduce)` block (lines 271-273)
   - Leave the rest of the reduced-motion media query intact (the `.hero-gradient` rule stays)

3. Do NOT touch `CardSpotlight.tsx` -- it is still actively used by ProjectCard.
  </action>
  <verify>
    <automated>cd "C:/Claude code projects/GSD Projects/Jack_Engineering_Portfolio" && test ! -f src/components/effects/NoisyBackground.tsx && test ! -f src/components/effects/AnimatedGridPattern.tsx && test ! -f src/components/effects/AuroraBackground.tsx && test ! -f src/components/effects/Particles.tsx && test -f src/components/effects/CardSpotlight.tsx && echo "PASS: 4 deleted, CardSpotlight kept"</automated>
  </verify>
  <done>4 deprecated effect files deleted. CardSpotlight.tsx preserved. Aurora CSS removed from app.css. No aurora keyframes or animate-aurora references remain.</done>
</task>

<task type="auto">
  <name>Task 2: Update test files and verify production build</name>
  <files>src/components/effects/__tests__/effects.test.ts, src/tests/bundle.test.ts, src/styles/__tests__/colors.test.ts</files>
  <action>
1. **Rewrite `src/components/effects/__tests__/effects.test.ts`** to only test CardSpotlight:
   ```typescript
   import { describe, it, expect } from 'vitest';
   import { CardSpotlight } from '../CardSpotlight';

   describe('effect component exports', () => {
     it('CardSpotlight exports as a function', () => {
       expect(typeof CardSpotlight).toBe('function');
     });
   });
   ```
   Remove all imports and tests for AuroraBackground, Particles, NoisyBackground, and AnimatedGridPattern.

2. **Update `src/tests/bundle.test.ts`** -- remove the test at lines 21-26 that asserts Contact.tsx imports LazyPdfViewer:
   ```typescript
   it('Contact imports LazyPdfViewer instead of PdfViewer', () => { ... })
   ```
   This entire test block should be deleted. The remaining 3 tests (LazyPdfViewer exists, PapersSection imports it, main.tsx imports font) are still valid and stay.

3. **Update `src/styles/__tests__/colors.test.ts`** -- remove the entire `describe('aurora keyframe in app.css', ...)` block (lines 89-101) which tests for `--animate-aurora`, `@keyframes aurora`, and `background-position`. The aurora CSS has been deleted so these assertions are no longer valid. All other color tests remain.

4. **Run the full test suite and production build** to verify everything is clean:
   ```bash
   npx vitest run --reporter=verbose && npx tsc -b --noEmit && npx vite build
   ```
  </action>
  <verify>
    <automated>cd "C:/Claude code projects/GSD Projects/Jack_Engineering_Portfolio" && npx vitest run --reporter=verbose 2>&1 | tail -20 && npx tsc -b --noEmit && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>All vitest tests pass. TypeScript compiles with no errors. Production build succeeds. No references to deprecated components (NoisyBackground, AnimatedGridPattern, AuroraBackground, Particles) remain in any test file or CSS. No admin code in production bundle.</done>
</task>

</tasks>

<verification>
1. `npx vitest run --reporter=verbose` -- all tests pass
2. `npx tsc -b --noEmit` -- no type errors
3. `npx vite build` -- production build succeeds
4. 4 effect component files no longer exist on disk
5. CardSpotlight.tsx still exists and its test passes
6. effects.test.ts only imports/tests CardSpotlight
7. bundle.test.ts has no Contact/LazyPdfViewer assertion
8. colors.test.ts has no aurora keyframe assertions
9. app.css has no aurora CSS (`--animate-aurora`, `@keyframes aurora`, `.animate-aurora`)
</verification>

<success_criteria>
- Zero deprecated component files in src/components/effects/ (only CardSpotlight.tsx remains)
- All vitest tests pass (no broken imports, no stale assertions)
- Production build succeeds cleanly
- No aurora CSS in app.css
- No references to NoisyBackground, AnimatedGridPattern, AuroraBackground, or Particles anywhere in the codebase except git history
</success_criteria>

<output>
After completion, create `.planning/phases/15-contact-footer-cleanup/15-02-SUMMARY.md`
</output>

---
phase: 06-static-assets-and-integration-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/resume.pdf
  - public/papers/lna-design.pdf
  - public/papers/mems-process-report.pdf
  - public/papers/fpga-fft.pdf
  - public/projects/lna-design.svg
  - public/projects/lna-schematic.svg
  - public/projects/mems-accelerometer.svg
  - public/projects/mems-sem.svg
  - public/projects/fpga-processor.svg
  - public/projects/fpga-block-diagram.svg
  - public/projects/adc-frontend.svg
  - public/projects/adc-pcb.svg
  - public/portrait.jpg
autonomous: true
requirements:
  - CONT-03
  - DOCS-01
  - DOCS-02
  - DOCS-04
  - PROJ-02

must_haves:
  truths:
    - "PdfViewer renders resume.pdf without hitting error fallback"
    - "PdfViewer renders each paper PDF with at least 2 pages for nav testing"
    - "All 4 project cards show thumbnail images without broken image icons"
    - "ProjectDetail dialog shows images[] without broken image icons"
    - "WhoAmI section shows the portrait image"
  artifacts:
    - path: "public/resume.pdf"
      provides: "Resume PDF for Contact section download and PdfViewer"
      contains: "PDF binary content"
    - path: "public/papers/lna-design.pdf"
      provides: "LNA Design paper for Papers section PdfViewer"
      contains: "PDF binary content"
    - path: "public/papers/mems-process-report.pdf"
      provides: "MEMS Process Report for Papers section PdfViewer"
      contains: "PDF binary content"
    - path: "public/papers/fpga-fft.pdf"
      provides: "FPGA FFT paper for Papers section PdfViewer"
      contains: "PDF binary content"
    - path: "public/projects/lna-design.svg"
      provides: "LNA project thumbnail"
      contains: "svg element with viewBox"
    - path: "public/projects/lna-schematic.svg"
      provides: "LNA schematic detail image"
      contains: "svg element with viewBox"
    - path: "public/projects/mems-accelerometer.svg"
      provides: "MEMS project thumbnail"
      contains: "svg element with viewBox"
    - path: "public/projects/mems-sem.svg"
      provides: "MEMS SEM detail image"
      contains: "svg element with viewBox"
    - path: "public/projects/fpga-processor.svg"
      provides: "FPGA project thumbnail"
      contains: "svg element with viewBox"
    - path: "public/projects/fpga-block-diagram.svg"
      provides: "FPGA block diagram detail image"
      contains: "svg element with viewBox"
    - path: "public/projects/adc-frontend.svg"
      provides: "ADC project thumbnail"
      contains: "svg element with viewBox"
    - path: "public/projects/adc-pcb.svg"
      provides: "ADC PCB detail image"
      contains: "svg element with viewBox"
    - path: "public/portrait.jpg"
      provides: "Portrait image for WhoAmI section"
      contains: "JPEG binary content"
  key_links:
    - from: "src/data/contact.ts"
      to: "public/resume.pdf"
      via: "contactData.resumePath = '/resume.pdf'"
      pattern: "resumePath.*resume\\.pdf"
    - from: "src/data/papers.ts"
      to: "public/papers/*.pdf"
      via: "paper.pdfPath field"
      pattern: "pdfPath.*papers/"
    - from: "src/data/projects.ts"
      to: "public/projects/*.svg"
      via: "project.thumbnail and project.images[] fields"
      pattern: "projects/.*\\.svg"
    - from: "src/components/sections/WhoAmI.tsx"
      to: "public/portrait.jpg"
      via: "PORTRAIT_SRC constant"
      pattern: "PORTRAIT_SRC.*portrait\\.jpg"
---

<objective>
Create all missing static assets in public/ that are referenced by data files and components but do not yet exist on disk.

Purpose: The portfolio is deployed and functional, but all PDF, SVG, and portrait image paths resolve to 404s. This plan fills every gap so PdfViewer renders real content, project cards show thumbnails, and the WhoAmI section shows a portrait.

Output: 13 new files in public/ (4 PDFs, 8 SVGs, 1 JPG placeholder)
</objective>

<execution_context>
@C:/Users/Jack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-static-assets-and-integration-fixes/06-RESEARCH.md

@src/data/projects.ts
@src/data/papers.ts
@src/data/contact.ts
@src/components/sections/WhoAmI.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create placeholder PDF files (resume + 3 papers)</name>
  <files>public/resume.pdf, public/papers/lna-design.pdf, public/papers/mems-process-report.pdf, public/papers/fpga-fft.pdf</files>
  <action>
Create the `public/papers/` directory. Then create 4 placeholder PDF files that are valid multi-page PDFs.

**Approach:** Use a Node.js script (run once, then delete) that generates minimal valid PDFs programmatically. Each PDF must have at least 3 pages so the PdfViewer page navigation controls (prev/next) are exercisable. Use the `pdf-lib` npm package (install as devDependency, remove after) OR write raw PDF bytes directly using a minimal PDF generator.

**Alternative simpler approach:** Use the `jspdf` package as a devDependency to create PDFs with actual text content:

For each PDF, include:
- **resume.pdf** (3+ pages): Page 1: "Jack Basinski - Resume" header + "Electrical and Computer Engineering, University of Washington" + placeholder sections (Education, Experience, Skills). Page 2: continued content. Page 3: continued content. Keep it realistic in structure.
- **lna-design.pdf** (3+ pages): Title page "Design of a Sub-2 dB NF LNA for 5G NR n77 Band", abstract page, body page with placeholder text.
- **mems-process-report.pdf** (3+ pages): Title page "Surface Micromachining of a Capacitive MEMS Accelerometer", abstract page, body page.
- **fpga-fft.pdf** (3+ pages): Title page "Pipelined FFT Architecture for Real-Time Spectrum Analysis", abstract page, body page.

Each PDF should include a footer note on every page: "PLACEHOLDER - Replace with actual document"

**Size constraint:** Each PDF under 100KB to avoid bloating the git repo.

After generating, remove any temporary script and devDependency used for generation (or leave jspdf as devDependency if removal would cause issues -- mark for cleanup).

**Paths must match exactly:**
- `public/resume.pdf` (matches contactData.resumePath = '/resume.pdf')
- `public/papers/lna-design.pdf` (matches papers[0].pdfPath)
- `public/papers/mems-process-report.pdf` (matches papers[1].pdfPath)
- `public/papers/fpga-fft.pdf` (matches papers[2].pdfPath)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['public/resume.pdf','public/papers/lna-design.pdf','public/papers/mems-process-report.pdf','public/papers/fpga-fft.pdf']; let ok=true; for(const f of files){if(!fs.existsSync(f)){console.error('MISSING:',f);ok=false;}else{const s=fs.statSync(f).size;console.log(f,s,'bytes');if(s<500){console.error('TOO SMALL:',f);ok=false;}}} if(!ok)process.exit(1); console.log('All PDFs exist and have reasonable size');"</automated>
  </verify>
  <done>4 PDF files exist at the exact paths referenced by data files. Each is a valid PDF with 3+ pages. All under 100KB.</done>
</task>

<task type="auto">
  <name>Task 2: Create placeholder SVG thumbnails and portrait image</name>
  <files>public/projects/lna-design.svg, public/projects/lna-schematic.svg, public/projects/mems-accelerometer.svg, public/projects/mems-sem.svg, public/projects/fpga-processor.svg, public/projects/fpga-block-diagram.svg, public/projects/adc-frontend.svg, public/projects/adc-pcb.svg, public/portrait.jpg</files>
  <action>
Create the `public/projects/` directory.

**SVG files (8 total):** Create engineering-themed placeholder SVGs. All must use `viewBox="0 0 640 360"` (16:9 ratio) to match the `aspect-video` CSS class used in ProjectCard's img element. Each SVG should:
- Have a light gray background (#F8F9FA)
- Include a simple geometric illustration relevant to the project domain
- Show the project name as text
- Include a subtle "PLACEHOLDER" watermark
- Be under 10KB each

Specific SVG content per project:

1. **lna-design.svg** (thumbnail): RF amplifier symbol -- a triangle (amplifier) with input/output lines, label "LNA Design"
2. **lna-schematic.svg** (detail): Schematic-style drawing with transistor symbol, inductors (coils), capacitors, label "LNA Schematic"
3. **mems-accelerometer.svg** (thumbnail): Comb-drive structure -- interleaved parallel lines representing capacitive fingers, label "MEMS Accelerometer"
4. **mems-sem.svg** (detail): Microscope view -- concentric circles suggesting SEM imaging, label "MEMS SEM Image"
5. **fpga-processor.svg** (thumbnail): Grid of small rectangles (logic blocks) connected by lines, label "FPGA Processor"
6. **fpga-block-diagram.svg** (detail): Block diagram -- rectangles labeled "FFT", "ADC", "Memory" connected by arrows, label "FPGA Block Diagram"
7. **adc-frontend.svg** (thumbnail): Op-amp triangle with differential inputs and filter waves, label "ADC Front-End"
8. **adc-pcb.svg** (detail): PCB trace pattern -- rectangular outline with trace lines inside, label "ADC PCB Layout"

Use the cleanroom color palette for accents: `#6C63FF` (UW purple accent) for key elements, `#1A1A2E` (ink) for text, `#F8F9FA` (cleanroom) for backgrounds.

**Portrait image (1 file):** Create `public/portrait.jpg` as a simple placeholder. Generate a minimal JPEG using a Node.js script with the `sharp` package (already in devDependencies from Phase 4 OG image generation). Create a 400x400 image with:
- Solid background color (#F8F9FA)
- Centered initials "JB" in dark text
- This is a valid JPEG that the WhoAmI component's img tag will render

**Size constraint:** Portrait under 50KB. SVGs under 10KB each.

**Exact paths (must match data files):**
- Thumbnails: `/projects/lna-design.svg`, `/projects/mems-accelerometer.svg`, `/projects/fpga-processor.svg`, `/projects/adc-frontend.svg`
- Detail images: `/projects/lna-schematic.svg`, `/projects/mems-sem.svg`, `/projects/fpga-block-diagram.svg`, `/projects/adc-pcb.svg`
- Portrait: `/portrait.jpg`
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const svgs=['public/projects/lna-design.svg','public/projects/lna-schematic.svg','public/projects/mems-accelerometer.svg','public/projects/mems-sem.svg','public/projects/fpga-processor.svg','public/projects/fpga-block-diagram.svg','public/projects/adc-frontend.svg','public/projects/adc-pcb.svg']; let ok=true; for(const f of svgs){if(!fs.existsSync(f)){console.error('MISSING:',f);ok=false;}else{const c=fs.readFileSync(f,'utf8');if(!c.includes('viewBox=\"0 0 640 360\"')){console.error('BAD VIEWBOX:',f);ok=false;}else{console.log('OK:',f);}}} if(!fs.existsSync('public/portrait.jpg')){console.error('MISSING: public/portrait.jpg');ok=false;}else{console.log('OK: public/portrait.jpg',fs.statSync('public/portrait.jpg').size,'bytes');} if(!ok)process.exit(1); console.log('All assets exist with correct dimensions');"</automated>
  </verify>
  <done>8 SVG files exist in public/projects/ with 16:9 viewBox, each showing engineering-themed placeholder illustrations. portrait.jpg exists in public/ as a valid JPEG. All rendered without broken image icons in browser.</done>
</task>

</tasks>

<verification>
1. All 13 static asset files exist at their expected paths in public/
2. `npx vite build` completes without errors (assets are served at build time)
3. `npx vitest --run` still passes (this plan does not modify source/test files)
</verification>

<success_criteria>
- 4 PDF files exist and are valid multi-page PDFs (3+ pages each, under 100KB)
- 8 SVG files exist with correct 16:9 viewBox matching aspect-video CSS
- 1 JPEG portrait file exists and is a valid image
- All file paths exactly match the references in projects.ts, papers.ts, contact.ts, and WhoAmI.tsx
- Production build (npx vite build) succeeds
- Existing test suite (npx vitest --run) still passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/06-static-assets-and-integration-fixes/06-01-SUMMARY.md`
</output>

---
phase: 07-requirements-traceability-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - FNDN-08
  - NAV-01
  - CRSE-01
  - CRSE-02
  - VISUAL-01
  - VISUAL-02
  - VISUAL-03
  - VISUAL-04
  - VISUAL-05
  - VISUAL-06
  - VISUAL-07

must_haves:
  truths:
    - "FNDN-08 is marked complete with Phase 5 attribution (21st.dev components were used in Phase 5)"
    - "NAV-01 requirement text accurately describes the 400px scroll threshold design and is marked complete with Phase 1 attribution"
    - "CRSE-01 and CRSE-02 are marked as descoped (not pending, not complete) with Phase 2 attribution"
    - "All seven VISUAL requirements (VISUAL-01 through VISUAL-07) have definition text in a Visual Design checkbox subsection"
    - "Coverage counts are arithmetically correct: 57 total, 54 complete, 1 deferred, 2 descoped, 0 pending"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Fully aligned requirements traceability document"
      contains: "VISUAL-01"
  key_links:
    - from: "checkbox section"
      to: "traceability table"
      via: "matching status values"
      pattern: "every [x] has Complete, every [-] has Descoped, every [ ] deferred has Deferred"
---

<objective>
Bring REQUIREMENTS.md into full alignment with the actual state of the codebase as established by the v1 milestone audit.

Purpose: The requirements document currently has 4 stale entries (FNDN-08, NAV-01, CRSE-01, CRSE-02 all showing incorrect statuses) and 7 missing definition entries (VISUAL-01 through VISUAL-07 have traceability rows but no checkbox definitions). This plan makes all corrections atomically so the document accurately reflects what was built, descoped, and satisfied.

Output: A single updated `.planning/REQUIREMENTS.md` with zero pending items, accurate coverage counts, and full checkbox-to-traceability consistency.
</objective>

<execution_context>
@C:/Users/Jack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-requirements-traceability-cleanup/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply all 7 edits to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read `.planning/REQUIREMENTS.md` in full, then apply all 7 edits described below atomically using the Write tool. Reference the 07-RESEARCH.md "Exact Edits Required" section for authoritative before/after values.

**Edit 1 — FNDN-08 checkbox (Foundation section):**
Change `- [ ] **FNDN-08**: 21st.dev MCP server used as primary source for sourcing premium React components`
to `- [x] **FNDN-08**: 21st.dev MCP server used as primary source for sourcing premium React components`

**Edit 2 — NAV-01 checkbox and text (Navigation section):**
Change `- [ ] **NAV-01**: User sees a fixed glassmorphic header with backdrop-blur, visible on all scroll positions`
to `- [x] **NAV-01**: User sees a fixed glassmorphic header with backdrop-blur that appears after scrolling past the hero (400px threshold)`

**Edit 3 — CRSE-01/CRSE-02 descoped (Coursework section):**
Change:
```
- [ ] **CRSE-01**: User sees a section highlighting key UW ECE courses
- [ ] **CRSE-02**: Courses include brief descriptors signaling domain relevance
```
to:
```
- [-] **CRSE-01**: ~~User sees a section highlighting key UW ECE courses~~ (descoped -- component built but excluded from rendered page by user decision)
- [-] **CRSE-02**: ~~Courses include brief descriptors signaling domain relevance~~ (descoped -- component built but excluded from rendered page by user decision)
```

**Edit 4 — Add Visual Design subsection (insert after Performance and Deployment section, before v2 Requirements):**
Add this new subsection:
```markdown
### Visual Design

- [x] **VISUAL-01**: UW purple color palette extension with oklch tokens and five effect components (Aurora, Particles, NoisyBackground, CardSpotlight, AnimatedGridPattern)
- [ ] **VISUAL-02**: Hero section displays animated aurora gradient with floating particles that respond to cursor movement (deferred -- user removed as too distracting)
- [x] **VISUAL-03**: WhoAmI, Skills, and Tooling sections have subtle noise texture backgrounds with purple tinting
- [x] **VISUAL-04**: Timeline has animated grid pattern background with engineering/technical aesthetic
- [x] **VISUAL-05**: Project cards show interactive spotlight effect following cursor on hover without breaking expand/collapse animations
- [x] **VISUAL-06**: Contact section uses subtle gradient background echoing hero aesthetic
- [x] **VISUAL-07**: Effect intensity follows bold hero to textured middle to calm footer curve across page
```

**Edit 5 — Traceability table row updates:**
Update these 4 rows in the traceability table:
- `FNDN-08`: change `Phase 7 | Pending` to `Phase 5 | Complete`
- `NAV-01`: change `Phase 7 | Pending` to `Phase 1 | Complete`
- `CRSE-01`: change `Phase 7 | Pending` to `Phase 2 | Descoped`
- `CRSE-02`: change `Phase 7 | Pending` to `Phase 2 | Descoped`
VISUAL-01 through VISUAL-07 rows are already correct -- do NOT modify them.

**Edit 6 — Coverage counts:**
Replace the entire Coverage block with:
```
**Coverage:**
- v1 requirements: 57 total (50 original + 7 VISUAL)
- Complete: 54
- Deferred: 1 (VISUAL-02 -- hero background effect, user decision)
- Descoped: 2 (CRSE-01, CRSE-02 -- coursework section, user decision)
- Pending: 0
- Unmapped: 0
```

**Edit 7 — Last updated timestamp:**
Change `*Last updated: 2026-03-20 after roadmap creation*`
to `*Last updated: 2026-03-24 after Phase 7 traceability cleanup*`

IMPORTANT: Make all 7 edits in a single Write operation. Do not make partial edits.
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*FNDN-08\*\*" .planning/REQUIREMENTS.md && grep -c "400px threshold" .planning/REQUIREMENTS.md && grep -c "\[-\] \*\*CRSE-01\*\*" .planning/REQUIREMENTS.md && grep -c "VISUAL-01" .planning/REQUIREMENTS.md && grep -c "Complete: 54" .planning/REQUIREMENTS.md && grep -c "2026-03-24" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>All 7 edits applied: FNDN-08 is [x], NAV-01 text rewritten and [x], CRSE-01/02 are [-] descoped, Visual Design subsection exists with 7 items, traceability table has correct phase/status for all 4 changed rows, coverage shows 54 complete / 1 deferred / 2 descoped / 0 pending, timestamp is 2026-03-24</done>
</task>

<task type="auto">
  <name>Task 2: Cross-validate checkbox-traceability consistency and coverage arithmetic</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read the updated `.planning/REQUIREMENTS.md` and perform a systematic consistency check. Do NOT modify the file unless errors are found.

**Check 1 — Checkbox-to-traceability agreement:**
For every requirement ID that appears in both the checkbox section and the traceability table, verify:
- `[x]` checkbox corresponds to `Complete` status in traceability
- `[-]` checkbox corresponds to `Descoped` status in traceability
- `[ ]` checkbox with "deferred" annotation corresponds to `Deferred` status
- Phase attribution in traceability matches where the work was actually done (NOT Phase 7)

Specific checks:
- FNDN-08: checkbox `[x]`, traceability `Phase 5 | Complete`
- NAV-01: checkbox `[x]`, text mentions "400px threshold", traceability `Phase 1 | Complete`
- CRSE-01: checkbox `[-]`, traceability `Phase 2 | Descoped`
- CRSE-02: checkbox `[-]`, traceability `Phase 2 | Descoped`
- VISUAL-01: checkbox `[x]`, traceability `Phase 5 | Complete`
- VISUAL-02: checkbox `[ ]` with deferred note, traceability `Phase 5 | Deferred`
- VISUAL-03 through VISUAL-07: checkbox `[x]`, traceability `Phase 5 | Complete`

**Check 2 — Coverage arithmetic:**
Count rows in traceability table by status:
- Complete rows should equal 54
- Deferred rows should equal 1
- Descoped rows should equal 2
- Pending rows should equal 0
- Total rows should equal 57
Verify the Coverage section numbers match these counts.

**Check 3 — No orphaned requirements:**
Every requirement ID in the checkbox section has a corresponding traceability row, and vice versa. No ID appears in one but not the other.

If any inconsistency is found, fix it. If all checks pass, report success.
  </action>
  <verify>
    <automated>grep -c "| Complete" .planning/REQUIREMENTS.md && grep -c "| Descoped" .planning/REQUIREMENTS.md && grep -c "| Deferred" .planning/REQUIREMENTS.md && grep -c "| Pending" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>All three consistency checks pass: (1) every checkbox status agrees with its traceability status, (2) coverage arithmetic is correct (54 + 1 + 2 + 0 = 57), (3) no orphaned requirements exist between checkbox and traceability sections</done>
</task>

</tasks>

<verification>
After both tasks complete, confirm all 5 ROADMAP success criteria:

1. FNDN-08 marked complete -- checkbox `[x]`, traceability `Phase 5 | Complete`
2. NAV-01 text updated -- says "appears after scrolling past the hero (400px threshold)", checkbox `[x]`, traceability `Phase 1 | Complete`
3. CRSE-01/CRSE-02 descoped -- checkbox `[-]` with strikethrough and annotation, traceability `Phase 2 | Descoped`
4. VISUAL-01-07 in traceability -- all 7 rows present with Phase 5, 6 Complete + 1 Deferred; all 7 have checkbox definition text in Visual Design subsection
5. Coverage counts accurate -- 57 total, 54 complete, 1 deferred, 2 descoped, 0 pending, 0 unmapped
</verification>

<success_criteria>
- REQUIREMENTS.md has zero pending items (was 4 pending before)
- All 57 requirements have both checkbox definition and traceability row
- Coverage counts are arithmetically verified
- No Phase 7 appears as attribution in any traceability row (work was done in Phases 1, 2, and 5)
- File timestamp updated to 2026-03-24
</success_criteria>

<output>
After completion, create `.planning/phases/07-requirements-traceability-cleanup/07-01-SUMMARY.md`
</output>

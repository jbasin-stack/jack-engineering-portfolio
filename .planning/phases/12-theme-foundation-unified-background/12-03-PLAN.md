---
phase: 12-theme-foundation-unified-background
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/admin/AdminShell.tsx
  - src/admin/AdminNav.tsx
  - src/admin/UploadZone.tsx
  - src/admin/editors/EditorSwitch.tsx
  - src/admin/editors/NavigationEditor.tsx
  - src/admin/editors/ProjectsEditor.tsx
  - src/admin/editors/PapersEditor.tsx
  - src/admin/editors/ToolingEditor.tsx
  - src/admin/editors/SkillsEditor.tsx
  - src/admin/editors/CourseworkEditor.tsx
  - src/admin/editors/TimelineEditor.tsx
  - src/admin/editors/ContactEditor.tsx
  - src/admin/editors/HeroEditor.tsx
  - src/admin/editors/shared/StructuredArrayField.tsx
  - src/admin/editors/shared/TagInput.tsx
  - src/admin/editors/shared/ItemList.tsx
  - src/admin/editors/shared/SectionHeader.tsx
autonomous: true
requirements:
  - THEME-02

must_haves:
  truths:
    - "Admin panel sidebar, header, and editor panels render with correct colors in both light and dark modes"
    - "No hardcoded bg-white, bg-gray-*, text-gray-*, or border-gray-* classes remain in admin component files"
    - "Skeleton loading states in editors are visible in both modes"
    - "shadcn components (Button, Input, Dialog) auto-theme correctly in admin context"
  artifacts:
    - path: "src/admin/AdminShell.tsx"
      provides: "Admin shell with semantic token classes"
    - path: "src/admin/AdminNav.tsx"
      provides: "Admin navigation with semantic token classes"
    - path: "src/admin/editors/shared/ItemList.tsx"
      provides: "Item list with semantic token classes"
  key_links:
    - from: "src/admin/**/*.tsx"
      to: "src/styles/app.css"
      via: "Tailwind semantic classes resolve to CSS variables which have .dark overrides"
      pattern: "bg-background|bg-card|bg-muted|text-foreground|text-muted-foreground|border-border"
---

<objective>
Replace all hardcoded light-mode color classes in admin panel components with Tailwind semantic tokens so the admin panel automatically themes correctly in both light and dark modes.

Purpose: The admin panel was built for light-mode-only dev use and has ~80 hardcoded bg-white/bg-gray/text-gray/border-gray references across ~16 files. These must be replaced with semantic tokens that respond to the .dark class established in Plan 01.
Output: Every admin component uses only semantic tokens for colors, rendering correctly in both modes.
</objective>

<execution_context>
@C:/Users/Jack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-theme-foundation-unified-background/12-CONTEXT.md
@.planning/phases/12-theme-foundation-unified-background/12-RESEARCH.md
@.planning/phases/12-theme-foundation-unified-background/12-01-SUMMARY.md

<interfaces>
<!-- Token mapping reference from 12-RESEARCH.md -->
<!-- Apply these replacements consistently across ALL admin files -->

bg-white           -> bg-background   (or bg-card for elevated panels/cards)
bg-gray-50         -> bg-muted
bg-gray-100        -> bg-muted
bg-gray-200        -> bg-muted        (or border-border for skeleton loaders)
text-gray-900      -> text-foreground
text-gray-700      -> text-foreground
text-gray-600      -> text-muted-foreground
text-gray-500      -> text-muted-foreground
text-gray-400      -> text-muted-foreground
border-gray-200    -> border-border
border-gray-100    -> border-border
hover:bg-gray-100  -> hover:bg-muted
hover:bg-gray-50   -> hover:bg-muted
hover:text-gray-600 -> hover:text-foreground
hover:text-gray-900 -> hover:text-foreground
bg-white/70        -> bg-background/70
bg-white/80        -> bg-background/80
bg-white/90        -> bg-background/90
bg-white/95        -> bg-background/95
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded colors in admin shell, nav, upload, and shared components</name>
  <files>src/admin/AdminShell.tsx, src/admin/AdminNav.tsx, src/admin/UploadZone.tsx, src/admin/editors/EditorSwitch.tsx, src/admin/editors/shared/StructuredArrayField.tsx, src/admin/editors/shared/TagInput.tsx, src/admin/editors/shared/ItemList.tsx, src/admin/editors/shared/SectionHeader.tsx</files>
  <action>
Open each file and apply the token mapping from the interfaces block above. This is a systematic find-and-replace operation across each file.

**AdminShell.tsx** (~3 occurrences of bg-white/bg-gray):
- Replace all bg-white with bg-background (or bg-card for the main panel area)
- Replace bg-gray-* with bg-muted
- Replace text-gray-* with text-foreground or text-muted-foreground (darker grays -> foreground, lighter grays -> muted-foreground)
- Replace border-gray-* with border-border

**AdminNav.tsx** (~1 occurrence):
- Same token mapping

**UploadZone.tsx** (~1 bg-white, ~8 text-gray/border-gray):
- The upload zone has multiple visual states (idle, drag, uploading). Replace all hardcoded gray classes.
- bg-white -> bg-card (elevated surface)
- text-gray-* -> text-muted-foreground or text-foreground
- border-gray-* -> border-border

**EditorSwitch.tsx** (~2 text-gray/bg-gray):
- Replace hardcoded colors with semantic tokens

**Shared components (StructuredArrayField, TagInput, ItemList, SectionHeader):**
- Each has 1-6 hardcoded gray references
- Apply the mapping consistently
- Pay special attention to hover states: hover:bg-gray-* -> hover:bg-muted

Do NOT change any non-color classes (spacing, layout, typography sizes, etc.). Only replace color-related utility classes.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; node -e "const fs=require('fs'); const files=['src/admin/AdminShell.tsx','src/admin/AdminNav.tsx','src/admin/UploadZone.tsx','src/admin/editors/EditorSwitch.tsx','src/admin/editors/shared/StructuredArrayField.tsx','src/admin/editors/shared/TagInput.tsx','src/admin/editors/shared/ItemList.tsx','src/admin/editors/shared/SectionHeader.tsx']; let bad=[]; files.forEach(f=>{try{const c=fs.readFileSync(f,'utf8'); if(/bg-white(?!\\s)/.test(c)||/bg-gray-/.test(c)||/text-gray-/.test(c)||/border-gray-/.test(c)){bad.push(f);}}catch(e){}}); if(bad.length){console.error('Still has hardcoded grays:',bad); process.exit(1);} console.log('All shell/shared components themed');"</automated>
  </verify>
  <done>AdminShell, AdminNav, UploadZone, EditorSwitch, and all shared editor components use semantic tokens. No hardcoded bg-white/bg-gray/text-gray/border-gray remain.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded colors in all editor components</name>
  <files>src/admin/editors/NavigationEditor.tsx, src/admin/editors/ProjectsEditor.tsx, src/admin/editors/PapersEditor.tsx, src/admin/editors/ToolingEditor.tsx, src/admin/editors/SkillsEditor.tsx, src/admin/editors/CourseworkEditor.tsx, src/admin/editors/TimelineEditor.tsx, src/admin/editors/ContactEditor.tsx, src/admin/editors/HeroEditor.tsx</files>
  <action>
Open each editor file and apply the same token mapping from the interfaces block.

**NavigationEditor.tsx** (~6 bg-white, ~8 text-gray/border-gray):
- This editor has the most hardcoded values. Systematically replace all.
- Skeleton loader bg-gray-200 -> bg-muted, bg-gray-100 -> bg-muted/50

**ProjectsEditor.tsx** (~3 bg-white, ~5 text-gray):
- Image preview cards: bg-white -> bg-card
- Labels and helper text: text-gray-* -> text-muted-foreground

**PapersEditor.tsx** (~3 bg-white, ~2 text-gray):
- Panel backgrounds: bg-white -> bg-card
- Status text: text-gray-* -> text-muted-foreground

**ToolingEditor.tsx** (~2 bg-white, ~2 text-gray):
- Same pattern

**SkillsEditor.tsx** (~2 bg-white, ~2 text-gray):
- Same pattern

**CourseworkEditor.tsx** (~2 bg-white, ~2 text-gray):
- Same pattern

**TimelineEditor.tsx** (~2 bg-white, ~2 text-gray):
- Same pattern

**ContactEditor.tsx** (~4 bg-white, ~4 text-gray):
- Skeleton loader animated pulse divs: bg-gray-200 -> bg-muted
- Panel backgrounds: bg-white -> bg-card

**HeroEditor.tsx** (~4 bg-white, ~4 text-gray):
- Skeleton loader animated pulse divs: bg-gray-200 -> bg-muted
- Panel backgrounds: bg-white -> bg-card

For skeleton loaders specifically: replace `bg-gray-200` with `bg-muted` and `bg-gray-100` with `bg-muted/50`. These provide enough visual distinction from bg-background in dark mode.

Do NOT change non-color classes. Only replace color-related Tailwind utilities.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; node -e "const fs=require('fs'); const files=['src/admin/editors/NavigationEditor.tsx','src/admin/editors/ProjectsEditor.tsx','src/admin/editors/PapersEditor.tsx','src/admin/editors/ToolingEditor.tsx','src/admin/editors/SkillsEditor.tsx','src/admin/editors/CourseworkEditor.tsx','src/admin/editors/TimelineEditor.tsx','src/admin/editors/ContactEditor.tsx','src/admin/editors/HeroEditor.tsx']; let bad=[]; files.forEach(f=>{const c=fs.readFileSync(f,'utf8'); if(/bg-white(?![a-z])/.test(c)||/bg-gray-/.test(c)||/text-gray-/.test(c)||/border-gray-/.test(c)){bad.push(f);}}); if(bad.length){console.error('Still has hardcoded grays:',bad); process.exit(1);} console.log('All editors themed');"</automated>
  </verify>
  <done>All 9 editor components use semantic tokens. No hardcoded bg-white/bg-gray/text-gray/border-gray remain anywhere in src/admin/.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` -- no TypeScript errors
- `npx vitest run -x` -- all tests pass
- Zero occurrences of bg-white, bg-gray-*, text-gray-*, border-gray-* in src/admin/ directory
- Admin panel renders with correct semantic token colors
</verification>

<success_criteria>
1. Zero hardcoded bg-white/bg-gray-*/text-gray-*/border-gray-* classes in any file under src/admin/
2. All admin components use semantic tokens (bg-background, bg-card, bg-muted, text-foreground, text-muted-foreground, border-border)
3. Skeleton loaders use bg-muted / bg-muted/50 for visibility in both modes
4. TypeScript compiles with no errors
5. All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-theme-foundation-unified-background/12-03-SUMMARY.md`
</output>

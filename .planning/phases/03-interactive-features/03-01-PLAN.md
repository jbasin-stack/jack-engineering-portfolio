---
phase: 03-interactive-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tsconfig.app.json
  - vite.config.ts
  - src/styles/app.css
  - src/types/data.ts
  - src/data/projects.ts
  - src/data/papers.ts
  - src/styles/motion.ts
  - src/hooks/useIsMobile.ts
  - src/data/__tests__/projects.test.ts
  - src/data/__tests__/papers.test.ts
  - src/styles/__tests__/motion.test.ts
  - src/components/ui/dialog.tsx
  - src/components/ui/drawer.tsx
  - public/pdf.worker.min.mjs
  - package.json
autonomous: true
requirements:
  - PROJ-05
  - DOCS-05

must_haves:
  truths:
    - "Project and Paper TypeScript interfaces exist and are exported"
    - "3-5 project entries exist in data file with all required fields including one featured"
    - "Paper entries exist in data file with title, descriptor, and pdfPath"
    - "shadcn Dialog and Drawer components are installed and importable"
    - "react-pdf is installed with worker copied to public/ for stable production path"
    - "Path alias @/ resolves in both TypeScript and Vite"
    - "useIsMobile hook exists and exports a function"
    - "Layout transition config exists in motion.ts without spring animations"
  artifacts:
    - path: "src/types/data.ts"
      provides: "Project and Paper interfaces"
      contains: "export interface Project"
    - path: "src/data/projects.ts"
      provides: "Typed project data array"
      exports: ["projects"]
    - path: "src/data/papers.ts"
      provides: "Typed paper data array"
      exports: ["papers"]
    - path: "src/hooks/useIsMobile.ts"
      provides: "Mobile detection hook"
      exports: ["useIsMobile"]
    - path: "src/styles/motion.ts"
      provides: "Layout transition config"
      contains: "layoutTransition"
    - path: "src/components/ui/dialog.tsx"
      provides: "shadcn Dialog component"
    - path: "src/components/ui/drawer.tsx"
      provides: "shadcn Drawer component"
    - path: "public/pdf.worker.min.mjs"
      provides: "PDF.js worker for production builds"
  key_links:
    - from: "src/data/projects.ts"
      to: "src/types/data.ts"
      via: "import type { Project }"
      pattern: "import type.*Project.*from.*types/data"
    - from: "src/data/papers.ts"
      to: "src/types/data.ts"
      via: "import type { Paper }"
      pattern: "import type.*Paper.*from.*types/data"
---

<objective>
Set up all prerequisites for Phase 3 interactive features: install and configure shadcn/ui (Dialog, Drawer), react-pdf with stable worker, path aliases, type interfaces (Project, Paper), data files, useIsMobile hook, and layout transition config.

Purpose: Provide the complete foundation that Plans 02 (Projects) and 03 (Papers/PDF) need -- types, data, UI primitives, and utilities -- so both can execute in parallel without setup overhead.
Output: Configured tooling, typed data layer, shadcn components, react-pdf worker, utility hook, and test scaffolds.
</objective>

<execution_context>
@C:/Users/Jack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-interactive-features/03-CONTEXT.md
@.planning/phases/03-interactive-features/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/data.ts (existing -- will be extended):
```typescript
export interface SocialLink {
  platform: string;
  url: string;
  icon: string;
}
// ... other existing interfaces (NavItem, HeroData, SkillGroup, etc.)
```

From src/styles/motion.ts (existing -- will be extended):
```typescript
import type { Variants } from 'motion/react';
export const easing = {
  out: [0.25, 1, 0.5, 1] as const,
  inOut: [0.65, 0, 0.35, 1] as const,
};
export const sectionVariants: Variants = { ... };
export const fadeUpVariant: Variants = { ... };
```

From src/styles/app.css (existing -- must preserve):
```css
@theme {
  --font-sans: 'Inter', ui-sans-serif, system-ui, sans-serif;
  --color-cleanroom: oklch(0.985 0.002 90);
  --color-ink: oklch(0.15 0.01 250);
  --color-accent: oklch(0.55 0.15 250);
  /* ... full palette */
}
```

From vite.config.ts (existing -- no path alias yet):
```typescript
export default defineConfig({
  plugins: [react(), tailwindcss()],
});
```

From tsconfig.app.json (existing -- no baseUrl/paths yet):
```json
{
  "compilerOptions": { /* ... no paths config */ }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, configure path aliases, initialize shadcn, and copy PDF worker</name>
  <files>package.json, tsconfig.app.json, vite.config.ts, src/styles/app.css, src/components/ui/dialog.tsx, src/components/ui/drawer.tsx, public/pdf.worker.min.mjs</files>
  <action>
1. Add path alias `@/` pointing to `./src` in both tsconfig.app.json and vite.config.ts:
   - tsconfig.app.json: add `"baseUrl": "."` and `"paths": { "@/*": ["./src/*"] }` to compilerOptions
   - vite.config.ts: add `import path from 'path'` and `resolve: { alias: { '@': path.resolve(__dirname, './src') } }` to the config

2. Install react-pdf: `npm install react-pdf`

3. Initialize shadcn/ui. Before running init, back up `src/styles/app.css`. Run `npx shadcn@latest init` and select:
   - Style: Default
   - Base color: Neutral (or Zinc -- closest to existing palette)
   - CSS variables: Yes
   - Tailwind v4: Yes (if prompted)
   After init, carefully restore the existing `@theme` block in `src/styles/app.css` -- the cleanroom palette tokens (`--color-cleanroom`, `--color-ink`, `--color-silicon-*`, `--color-accent`, `--font-sans`, `--tracking-architectural`, `--ease-*`) MUST be preserved. The shadcn CSS variables (`--background`, `--foreground`, `--primary`, etc.) can coexist alongside them. Map shadcn's `--background` to the cleanroom white value and `--foreground` to ink. If shadcn overwrites `app.css`, merge manually from the backup. Preserve the `@layer utilities` block with `.border-hairline` and the body styles.

4. Add shadcn Dialog and Drawer components: `npx shadcn@latest add dialog drawer`

5. Copy the PDF.js worker to public/ for stable production path:
   `cp node_modules/pdfjs-dist/build/pdf.worker.min.mjs public/pdf.worker.min.mjs`

6. Verify: `npx tsc -b --noEmit` passes (path aliases resolve), `npm run build` succeeds, existing tests still pass with `npx vitest run`.

IMPORTANT: After shadcn init, verify that the existing components and styles still work. The `src/styles/app.css` file is the most critical -- any loss of the cleanroom palette tokens will break the entire site. If shadcn creates a new CSS file (like `globals.css`), consolidate everything into `app.css` and delete the extra file.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit && npx vitest run --reporter=verbose && npm run build</automated>
  </verify>
  <done>Path aliases @/ resolve in TS and Vite. shadcn Dialog and Drawer components exist in src/components/ui/. react-pdf installed. PDF worker at public/pdf.worker.min.mjs. Existing cleanroom palette preserved in app.css. All existing tests pass. Production build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create Project and Paper interfaces, data files, useIsMobile hook, layout transition config, and test scaffolds</name>
  <files>src/types/data.ts, src/data/projects.ts, src/data/papers.ts, src/hooks/useIsMobile.ts, src/styles/motion.ts, src/data/__tests__/projects.test.ts, src/data/__tests__/papers.test.ts, src/styles/__tests__/motion.test.ts</files>
  <action>
1. Add Project and Paper interfaces to src/types/data.ts (append to existing file, do not modify existing interfaces):

```typescript
export interface Project {
  id: string;
  title: string;
  brief: string;          // One-line card description
  summary: string;        // Expanded view paragraph (objective, methods, results)
  thumbnail: string;      // Path to stylized graphic in public/
  images: string[];       // Gallery images for detail view
  domain: string;         // "RF" | "Fabrication" | "Analog" | "Digital" -- aligns with skill domains
  techStack: string[];    // Tech stack tags
  links: { label: string; url: string }[];
  featured: boolean;      // Spans 2 columns in bento grid
  paperPdf?: string;      // If project links to a paper PDF, opens PDF viewer instead of Dialog
}

export interface Paper {
  id: string;
  title: string;
  descriptor: string;     // Course/publication context
  pdfPath: string;        // Path to PDF in public/papers/
}
```

2. Create src/data/projects.ts with 4 project entries (placeholder content representing realistic ECE projects -- Jack can update the text later). One project must have `featured: true`. Domains should span at least 3 of the 4 categories (RF, Fabrication, Analog, Digital). Use placeholder thumbnail paths like `/projects/project-name.svg`. Example projects:
   - A featured RF project (e.g., low-noise amplifier design)
   - A fabrication project (e.g., MEMS accelerometer)
   - A digital project (e.g., FPGA-based signal processor)
   - An analog project (e.g., precision ADC front-end)

3. Create src/data/papers.ts with 2-3 paper entries. Use placeholder PDF paths like `/papers/paper-name.pdf`. Example papers:
   - An RF research paper
   - A fabrication process report

4. Create src/hooks/useIsMobile.ts with the `useIsMobile` hook using `window.matchMedia` at 768px breakpoint (aligns with Tailwind `md` breakpoint). Use the pattern from RESEARCH.md: `useState`, `useEffect`, `matchMedia`, `addEventListener('change')`. Export as named export `useIsMobile`.

5. Add `layoutTransition` config to src/styles/motion.ts (append, do not modify existing exports):

```typescript
// Layout animation transition for card expand/collapse
export const layoutTransition = {
  duration: 0.4,
  ease: easing.out,
};
```

6. Create test scaffolds:

src/data/__tests__/projects.test.ts:
- Projects is an array of 3-5 entries
- Each project has all required fields (id, title, brief, summary, thumbnail, images, domain, techStack, links, featured)
- Exactly one project has featured: true
- Domain values are one of RF, Fabrication, Analog, Digital
- All string fields are non-empty
- techStack is a non-empty array of non-empty strings

src/data/__tests__/papers.test.ts:
- Papers is an array of 2+ entries
- Each paper has all required fields (id, title, descriptor, pdfPath)
- All string fields are non-empty
- pdfPath starts with '/'

Extend src/styles/__tests__/motion.test.ts:
- Add `layoutTransition` to the imports
- Add test: `layoutTransition has duration and ease defined (confirms tween)`
- Add `layoutTransition` to the deepCheckForSpring allConfigs array so the no-spring test covers it
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Project and Paper interfaces exported from data.ts. 4 projects in projects.ts with one featured. 2-3 papers in papers.ts. useIsMobile hook exported. layoutTransition in motion.ts. All new and existing tests pass (no spring violations).</done>
</task>

</tasks>

<verification>
- `npx tsc -b --noEmit` -- TypeScript compiles with no errors (path aliases, new types)
- `npx vitest run --reporter=verbose` -- All tests pass including new data shape tests and updated motion spring check
- `npm run build` -- Production build succeeds (shadcn, react-pdf, worker in public/)
- Verify `src/components/ui/dialog.tsx` and `src/components/ui/drawer.tsx` exist
- Verify `public/pdf.worker.min.mjs` exists
- Verify existing site appearance unchanged (cleanroom palette preserved)
</verification>

<success_criteria>
- shadcn Dialog and Drawer installed and importable via `@/components/ui/dialog` and `@/components/ui/drawer`
- react-pdf installed, worker at stable public/ path
- Project and Paper types exported, data files populated with 4 projects (1 featured) and 2-3 papers
- useIsMobile hook ready for Dialog/Drawer responsive pattern
- layoutTransition config ready for card animations, tween-only (no spring)
- All tests green (existing + new data shape + motion spring check)
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-interactive-features/03-01-SUMMARY.md`
</output>
